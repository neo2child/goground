package main

import "fmt"

func arrayTest() {
	names := []string{"volvo", "benz", "bmw"}
	for i := 0; i < len(names); i++ {
		fmt.Println(names[i])
	}
}

func arrayTest() {
	names := []string{"volvo", "benz", "bmw"}
	for i := 0; i < len(names); i++ {
		fmt.Println(names[i])
	}
}

func main() {
	arrayTest()
}
